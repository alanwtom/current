# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Xcode
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcuserstate
DerivedData/

# App bundles produced by Scripts/make-app.sh
*.app

# macOS
.DS_Store

# Intermediate icon artwork — regenerate with: swift Scripts/make-icon.swift
Scripts/AppIcon.iconset/

# The signed disk image ships to Vercel, not into git history.
site/Current.dmg

# Session-local editor/agent config, not part of the project.
.claude/

# Working notes: design audits, unsent drafts, anything written to me rather
# than to someone reading the repo. These were public once; keep them local.
plans/
